import { TimeRegionExtraServices } from './time-region-extra-services';

describe('TimeRegionDayServices', () => {
  it('should create an instance', () => {
    expect(new TimeRegionExtraServices()).toBeTruthy();
  });
});
